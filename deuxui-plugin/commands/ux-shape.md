---
description: Decide what to build before any code exists — returns a confirmed brief and writes nothing else.
argument-hint: "<the feature or surface>"
---

Target: $ARGUMENTS

Invoke the `deuxui` skill and follow `references/ops/shape.md`.

This operation writes no code. It ends with a brief the user has confirmed, and
if you find yourself editing a component you have left the operation.

Two or three questions per round, then stop and wait. One round is the default.
Do not send a questionnaire, do not re-ask what the brief already settled, and do
not turn an obvious fact into a menu — assert the likely reading and invite
correction.

Name the visitor mode for this surface before anything else
(`references/design/visitor-modes.md`), because it decides what "good" means here,
and record the rare high-impact tasks — cancel, refund, delete, recover — since
those are the ones a brief written from the happy path always omits.
