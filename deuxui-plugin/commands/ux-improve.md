---
description: Improve existing UI — polish, redesign or simplify, preserving what already works.
argument-hint: "[path or route]"
---

Run the deuxui `improve` workflow on $ARGUMENTS.

Invoke the `deuxui` skill, then follow its `improve` workflow (`references/workflows/improve.md`).

Climb the preserve ladder before writing anything: `ux_check.py --inventory <intent>`
lists the primitives that already exist. Reuse beats modify, modify beats compose,
compose beats create. Record which you chose and why.

Take a baseline screenshot first, so you can prove the parts you did not mean to touch
did not move.
