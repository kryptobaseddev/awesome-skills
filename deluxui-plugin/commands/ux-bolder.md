---
description: Raise one named part to the conviction the rest already implies — in the system's own vocabulary.
argument-hint: "<the target, and what must stay untouched>"
---

Target: $ARGUMENTS

Invoke the `deluxui` skill and follow `references/ops/bolder.md`.

Scope is sovereign. Touch only the named target. Do not add a colour, face,
radius, shadow or primitive the surface does not already own — if the system
genuinely cannot express the direction, stop and ask, naming the exact addition
and the job it would do.

Reject the reflex answer first. More effects is louder, not bolder, and it is what
`S-SLOP-GRADIENT`, `S-CRAFT-HALO`, `S-SLOP-BLUR` and `S-CRAFT-HARD-SHADOW` exist
to catch.

Apply the skeleton test before you finish: strip the copy out and check that the
bare structure still says what this section is.
